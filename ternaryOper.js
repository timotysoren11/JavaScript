// Checking the age of person if he or she are allowed to drive

let age = 16;
let message;

// Condition using ternary operator
age >= 16 ? (message = 'You can drive') : (message = 'You cannot drive');

//printing the message
console.log(message);

