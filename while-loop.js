// Example of while-loop
let count = 1;

// it loops until it reaches to close to 10
while(count < 10){
    console.log(count); //each time the number is printed

    count += 2; // each time the number is being added by 2 
}
