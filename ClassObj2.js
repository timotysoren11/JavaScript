// Defining the values of property in the constructor

// Creating the class person
class person {
  constructor(n,a){
    this.name = n;
    this.age = a;
  }
}

// creating the object
let person1 = new person('Raju',28);

// displaying the result
console.log(person1);